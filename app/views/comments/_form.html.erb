<%= form_with(model: [post, post.comments.build]) do |f| %>
    <div class="form-contrl">
        <%= f.rich_text_area :body %>
        <%= f.submit "Reply", disable_with: 'Replying...', class: "btn btn-primary mt-1" %>
    </div>
<% end %>